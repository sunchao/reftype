

extend-set : set -> nat -> set -> set -> type.

extend-set/in
  : set`not-member R' N
    -> set`add R' N R
    -> set`union S R' Q
    -> extend-set S N R Q.

extend-set/out
  : set`not-member R N
    -> extend-set _ N R R.

